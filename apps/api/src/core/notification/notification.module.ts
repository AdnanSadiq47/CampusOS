import { Module } from '@nestjs/common';

/**
 * Architectural Boundary: Notification Engine Module
 * (Multi-Channel Dispatcher: Email, SMS, Webhooks, In-App)
 */
@Module({})
export class NotificationModule {}
