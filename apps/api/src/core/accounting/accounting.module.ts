import { Module } from '@nestjs/common';

/**
 * Architectural Boundary: Double-Entry Accounting Module
 * (General Ledger, Chart of Accounts, Vouchers, Period Locking)
 */
@Module({})
export class AccountingModule {}
